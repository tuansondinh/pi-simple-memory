{
  "name": "pi-claude-memory",
  "version": "0.1.0",
  "description": "Persistent file-based memory for pi agents. Ported from the LSD memory extension.",
  "type": "module",
  "keywords": ["pi-package", "pi", "memory"],
  "license": "MIT",
  "pi": {
    "extensions": ["src/index.ts"]
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "*",
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
