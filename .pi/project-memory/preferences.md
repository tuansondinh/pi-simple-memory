# Preferences

## User runs pi extensions via `packages` array in ~/.pi/agent/settings.json, using <!-- id:e708 -->
_Added: 2026-04-23T16:34:42.207Z | Type: preference | Category: workflow | Source: agent-tool_

User runs pi extensions via `packages` array in ~/.pi/agent/settings.json, using relative paths like `../../Workspace/pi-xyz` for local dev forks

---
